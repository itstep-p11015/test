1. ¬ каждом окне создать массив с 30 случайными числами от 0 до 100.
2. ќтобразить массив с помощью столбиков
3. ¬ отдельном потоке сортировать методом со сложностью O(N^2) массив, соверша€ ~1 операцию в секунду, отрисовыва€ состо€ние массива на форме.
4. —делать, чтобы помимо медленной сортировки, можно было наблюдать и быструю.